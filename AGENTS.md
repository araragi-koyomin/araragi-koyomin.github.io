# AGENTS.md

## 适用范围

本文件适用于仓库根目录及其全部子目录。若未来某个子目录新增更具体的
`AGENTS.md`，则该文件只覆盖其所在子树；未被覆盖的规则继续生效。

## 项目定位

这是一个部署到 GitHub Pages 的个人网站，源码分支为 `main`，发布产物位于
`gh-pages`。站点基于 Astro 5 和 AstroPaper，目标是逐步建设为中文个人博客与
学习笔记站点，同时保持静态、轻量、可维护和可恢复。

项目目标、需求基线和开放问题见 `docs/PROJECT.md`；任何进行中的事项以
`docs/BACKLOG.md` 为唯一热层索引。

## 安全与 Git 边界

- 完整保留用户已有的 staged、unstaged、untracked 和删除状态；不得把未知改动
  视为错误、模板噪声或可自动恢复内容。
- 未经用户明确授权，不得执行 `git pull`、`git push`、提交、切换分支、部署、
  强制覆盖、重新克隆或清理工作区。
- 禁止使用 `git reset --hard`、`git checkout --`、`git clean` 等破坏性命令。
- 所有项目命令必须在 `E:\website1` 下执行。
- 安装依赖、构建和测试如果会生成或更新 `.astro`、`dist`、`public/pagefind`、锁文件
  或其他项目状态，必须先说明影响并获得授权。
- 不读取、输出或提交 `.env`、令牌、私钥等秘密。

## 文档位置

- 新产生的开发文档必须放在 `docs/` 下，不得放入 `src/pages`、`src/data/blog`、
  `public` 或其他会成为 GitHub Pages 内容的目录。
- 根目录 `AGENTS.md` 是开发文档位置规则的唯一例外。
- 现有根目录 `README.md` 是仓库入口文档，不作为开发过程记忆层使用。
- 不为简单改动创建多份重复文档。项目级事实写入 `docs/PROJECT.md`；只有跨多个
  文件、多个回合或存在长期设计约束的工作才新建独立温层文档。

## 三层记忆架构

### 热层：`docs/BACKLOG.md`

- 只保存活跃项，状态只能是 `ready`、`in_progress` 或 `blocked`。
- 它是当前范围、优先级、依赖和状态的唯一索引，不是完成日志。
- 事项完成、取消或被替代后，必须从 BACKLOG 移除，并按需进入冷层归档。

### 温层：活跃文档

- 位于 `docs/`，例如 `docs/PROJECT.md`、`docs/DOCUMENTATION.md` 或未来的
  `docs/plans/*.md`。
- 保存仍在生效的需求、计划、约束、验收标准和决策上下文。
- 温层内容发生范围、状态或决策变化时，必须同步更新其 frontmatter 和正文。

### 冷层：完成归档

- 位于 `docs/archive/<年份>/`。
- 保存已完成、已取消或已被替代的计划、审计、报告和决策；归档不等于删除。
- 冷层文档保留完成证据和历史上下文，不重新作为活跃计划使用；若工作重新启动，
  应创建新的活跃项并链接旧归档。

## Frontmatter 规则

所有 `docs/**/*.md`（包括 `docs/BACKLOG.md`）必须从第 1 行开始包含有效 YAML
frontmatter。至少包含：

```yaml
---
title: 文档标题
description: 一句话说明用途
type: backlog | project | governance | plan | decision | audit | report
layer: hot | warm | cold
status: active | ready | in_progress | blocked | completed | cancelled | superseded
updated: YYYY-MM-DD
---
```

- 冷层文档还必须包含 `archived_at: YYYY-MM-DD`。
- 新建文档时应按需加入 `created`、`related`、`owners` 等字段，但不得用空字段占位。
- frontmatter 的状态必须和正文、BACKLOG 一致。

## BACKLOG 强制同步事件

以下事件发生时必须同步更新 `docs/BACKLOG.md`，不可遗漏：

- 收到任何新需求、功能、缺陷、风险、技术债或调查任务。
- 开始新的功能开发或恢复一个暂停事项。
- 需求范围、优先级、依赖、验收标准或负责人发生变化。
- 事项进入 `in_progress`、`blocked`，或解除阻塞。
- 新建、启用、归档或替代温层文档。
- 完成功能、取消需求或确认不再处理某项工作。

执行顺序：

1. 新需求到达后，先更新 BACKLOG，再修改实现文件。
2. 开始工作时，将对应项标记为 `in_progress`。
3. 工作中发现新需求或新风险时，立即新增或更新 BACKLOG 项。
4. 完成并验证后，更新温层文档和完成证据，必要时移入冷层，然后从 BACKLOG
   删除该活跃项。
5. 最终交付前再次核对 BACKLOG，确保状态与代码、文档、测试结果一致。

## 需求澄清与复杂度控制

- 能从代码、文档、Git 历史或现有配置查明的事实，应先调查，不重复询问用户。
- 若歧义会改变产品目标、公开内容、语言、身份信息、架构、部署方式、兼容性、
  安全性或工作量，必须立即提出，并把相关 BACKLOG 项标为 `blocked`；在确认前
  不实现受影响范围。
- 个人博客优先选择静态、低维护、少依赖方案。若方案引入服务端、数据库、CMS、
  登录系统、付费服务、复杂状态管理或大规模设计系统，应先说明收益、维护成本和
  更简单替代方案，再请求确认。

## 沟通与解释纪律

项目检查、需求分析、方案设计、代码开发、测试验证、问题审查和最终汇报都必须让
没有相关工程经验的用户能够据此理解问题并作出决定。不能为了简短而省略必要背景。

- 阶段更新和最终回复应按实际需要说明：背景、可公开验证的事实或代码证据、影响
  范围、当前状态、未验证项和下一步。不得只报告“已阻塞”“已闭环”“Red/Green”
  等状态词。
- 优先使用日常、常见、直接的中文。可以说清楚具体事实时，不得改写成自造术语、
  自造缩写或小众术语。
- 专业术语、英文缩写和事项编号第一次出现时，必须立即解释：它是什么、在本项目中
  对应哪段行为、为什么重要、出问题会造成什么后果。适合时给出与当前网站直接相关
  的例子。
- 需要用户选择方案时，必须分别说明每个方案会修改什么、不会修改什么、优点、缺点、
  风险、工作量和推荐理由。用户不应在不了解术语或后果的情况下确认方案。
- 测试失败时应说明哪个测试在什么条件下为什么失败；代码修改后应说明具体改了什么；
  尚未完成时应说明缺少哪项证据或决定。若使用测试驱动开发中的 Red/Green，第一次
  必须解释为“先写能复现缺陷的失败测试”和“实现修复后测试通过”。
- 审查结论必须翻译成用户可验证的行为。例如不能只说“权限边界不完整”，而要说明
  哪类用户能通过哪个页面或请求获得哪项本不应拥有的能力。
- 如果用户表示没有看懂，应暂停依赖该理解的重大方案，从头使用更常见的语言重新
  解释，不得只替换术语后再次要求确认。
- 结论应公开说明可验证的判断依据和设计取舍；不需要展示不可见的内部推理过程，
  但不能省略支撑结论的代码、测试、文档或事实证据。

## 标准工作流

1. 阅读本文件、`docs/BACKLOG.md` 和该事项引用的温层文档。
2. 检查 Git 状态，识别用户已有改动，确定本次允许修改的文件范围。
3. 更新 BACKLOG，记录需求、状态、依赖和验收标准。
4. 调查足够证据；遇到影响范围的歧义立即询问。
5. 实施最小且完整的变更，避免覆盖无关用户改动。
6. 按风险执行验证；会改变项目状态的验证先取得授权。
7. 同步更新温层文档、BACKLOG 和必要的冷层归档。
8. 向用户报告改动、验证结果、未验证项、风险和仍需决定的问题。

## 完成定义

一个事项只有在以下条件满足后才算完成：

- 需求和验收标准均已满足。
- 相关代码、内容和配置保持一致。
- 已执行获准的验证，或明确记录无法验证的原因。
- 没有意外覆盖现有工作区修改。
- 温层文档已更新；完成证据已归档；BACKLOG 中已移除完成项。
